return {
    Vect2 = Vector2.new;
    Dim2 = UDim2.new;
    Dim = UDim.new;
    RGB = Color3.fromRGB;
    RGBKey = ColorSequenceKeypoint.new;
    RGBSeq = ColorSequence.new;
    NumKey = NumberSequenceKeypoint.new;
    NumSeq = NumberSequence.new;
    Abs = math.abs;
    Ceil = math.ceil;
    Floor = math.floor;
    Round = math.round;
    Clamp = math.clamp;
    Min = math.min;
    Max = math.max;
    Random = math.random;
    Sin = math.sin;
    Cos = math.cos;
    Tan = math.tan;
    TableFind = table.find;
    TableInsert = table.insert;
    TableRemove = table.remove;
    TableSort = table.sort;
    TableClear = table.clear;
    TableConcat = table.concat;
    TablePack = table.pack;
    TableUnpack = table.unpack;
}
